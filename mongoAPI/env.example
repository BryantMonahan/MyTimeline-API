MONGODB_URI=
ENVIRONMENT="development" | "production"
S3_REGION=
S3_BUCKET_NAME=
# Only needed for local development. In production, use an IAM role
# (EC2 instance profile / ECS task role) instead of long-lived keys.
AWS_ACCESS_KEY=
AWS_SECRET_ACCESS_KEY=